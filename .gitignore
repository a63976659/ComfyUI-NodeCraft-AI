# ─── NodeCraft AI 忽略清单 ───────────────────────────────
# 运行时数据（会话、审计日志、密钥、本地模型环境等）
数据/

# 运行日志
logs/

# 工具缓存
.ruff_cache/
__pycache__/
*.py[cod]

# SQLite 临时文件
*.db-shm
*.db-wal

# 一次性调查/分析脚本（排障时临时写的日志统计、阈值试算等，不属于项目代码）
_tmp_*
