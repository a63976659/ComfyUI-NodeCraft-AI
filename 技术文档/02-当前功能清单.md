# 当前功能清单

> 本文档记录项目已实现、部分实现和未实现的功能状态。  
> 最后更新时间：2026-06-22

---

## 已实现功能 ✅（78 项）

### 基础交互与 UI

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ 侧边栏 UI 注册与渲染 | 向 ComfyUI 注册侧边栏面板，渲染聊天界面 | 前端 |
| ✅ 基础聊天对话 | 用户发送消息，AI 返回回复，展示在界面上 | 全链路 |
| ✅ Markdown 简易渲染 | 代码块（带语言标签）、复制按钮、粗体、链接 | 前端 |
| ✅ 工具介绍页 | 品牌展示、特性卡片、GitHub 链接 | 前端 |
| ✅ 三标签页多界面架构 | 开发插件/优化插件/功能可视化，Tab 切换 | 前端 |
| ✅ 文件夹选择器 | 列表展示 custom_nodes 插件 + 搜索过滤 + 外部装载 | 前端 |
| ✅ 模型选择栏（三面板同步） | 三个面板各有独立模型选择栏，共享全局状态 | 前端 |
| ✅ 项目信息栏 | 显示文件夹名称 + 打包按钮 + 从模板创建按钮（新建项目按钮已整合至会话栏＋按钮） | 前端 |
| ✅ 会话栏标题区按钮整合 | 标题栏右侧整合「＋ 新建项目」（展开会话栏 + 弹出创建项目对话框 + 自动创建同名会话）与「✕ 取消会话」（恢复欢迎页）两个动作按钮 | 前端 |
| ✅ 会话标题 inline 编辑 | 每个会话项删除按钮旁有 ✎ 编辑按钮，点击 inline 编辑，Enter 保存，Escape 取消 | 全链路 |
| ✅ 折叠状态当前会话标题显示 | 会话栏折叠时显示当前选中会话标题（accent 色，省略号截断），无选中则不显示 | 前端 |
| ✅ 模型状态栏全局固定 | 底部固定显示模型状态，切换面板不影响 | 前端 |
| ✅ 版本信息栏全局固定 | 底部固定显示版本号，切换面板不影响 | 前端 |
| ✅ 项目创建对话框 | 英文名验证、路径预览 | 前端 |
| ✅ 响应式设计 | 平板（≤768px）+ 手机（≤480px）断点适配 | 前端 |
| ✅ 星星按钮独立登录面板 | 右上角✦按钮点击弹出全屏覆盖登录面板（nca-overlay + nca-panel 风格，与设置面板一致），内容通过 .nca-login-center-wrap 居中展示（max-width: 480px）；设置面板移除"账号"Tab，登录入口完全独立于设置面板 | 前端 |
| ✅ 登录界面双入口统一 | 默认入口与星星按钮入口 UI 完全一致，均无关闭按钮，登录成功后统一显示"进入 →"按钮 | 前端 |
| ✅ 登录流程与布局优化 | 默认入口改为登录界面，登录后通过"进入 →"按钮进入主界面；内容区域整体上移、增大间距；退出登录按钮小型化设计 | 前端 |
| ✅ 登录面板浅色模式适配 | .nca-theme-light 全量 CSS 覆盖（卡片背景、品牌文字、Tab 切换、输入框、按钮等所有元素） | 前端 |
| ✅ 文件夹选择标签事件总线同步 | 选中状态统一通过事件总线「插件选择变更」广播，设置插件文件夹() 为唯一入口，刷新/切换会话时标签自动同步 | 前端 |
| ✅ 会话列表面板公共组件 | `前端/会话列表面板.js` 统一封装三个面板（开发/优化/可视化）的会话列表 UI，通过配置参数差异化行为（type、showSearch、showCancelButton 等） | 前端 |
| ✅ 优化/可视化面板会话列表独立化 | 三界面各有独立会话列表，通过 `type` 字段隔离；优化/可视化面板不显示搜索框（showSearch: false） | 全链路 |
| ✅ 会话标题栏取消按钮 | 三个面板标题栏均增加取消按钮（×），点击清除会话选择并恢复欢迎页 | 前端 |
| ✅ 插件文件夹与会话互斥选择 | 优化/可视化面板中插件列表选择与会话文件夹选择互斥，`最后文件夹来源` 状态跟踪，选一方自动清另一方，状态栏显示最终生效文件夹 | 前端 |
| ✅ 释放显存按钮统一（三面板） | 三个面板均支持本地模型释放显存按钮，通过共享函数 `创建模型切换栏副本` 复用逻辑 | 前端 |

### 模型与推理

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ API 模型工具调用循环上限（15 轮） | 非流式和流式均支持最多 15 轮工具调用（从 5 轮提升），支持复杂多文件读写场景；提示信息动态引用 `最大工具循环次数` 变量 | 智能体 |
| ✅ 本地模型工具调用适配层 | 通过 `<tool_call>` XML 标记让 transformers 本地模型触发文件工具；非流式/流式均支持最多 15 轮循环；前端过滤工具标记 | 智能体/后端/前端 |
| ✅ LLM API 调用（OpenAI 兼容） | 通过 OpenAI 兼容 API 调用大语言模型 | 智能体 |
| ✅ 本地模型推理 | transformers 加载、GPU auto 分配、异步推理 | 智能体 |
| ✅ SSE 流式响应 | API/本地双模式流式、打字机效果、停止生成按钮 | 全链路 |
| ✅ WebSocket 实时通信 | GET /ai-coder/ws，认证流程，心跳 30s，指数退避重连 | 全链路 |
| ✅ 本地模型扫描 | /ai-coder/local-models 端点，扫描 models/LLM 目录 | 后端 |
| ✅ 模型切换 UI | 本地/API 切换按钮、本地模型下拉、API 信息展示 | 前端 |
| ✅ 模型量化加速 | 4-bit/8-bit BitsAndBytes 量化、Flash Attention 2、torch.inference_mode() | 智能体 |
| ✅ BM25 检索升级 | 纯 Python BM25 引擎、bigram 分词、68 段文档索引、LRU 缓存 | 智能体 |
| ✅ BM25 多实例（按 Tab 隔离） | 全局单例升级为 `{develop, optimize, visualize}` 字典，每 Tab 独立加载子目录 `知识库/{TAB_FOLDER_MAP[tab]}/`（映射为开发插件/优化插件/功能可视化），按需加载 | 智能体 |
| ✅ AI 自动检索踩坑记录 | BM25 索引扩展加载 `数据/踩坑记录/*.json`，doc_id 前缀 `faq://`，检索结果分离为 KB(top-3) + FAQ(top-2)，仅在 develop / optimize Tab 中加载 | 智能体 |
| ✅ 滑动窗口上下文压缩 | tiktoken 编码，4000 token 限制，超出则截断旧消息 | 智能体 |
| ✅ Tab 差异化系统提示词 | develop → "ComfyUI 插件开发专家"；optimize → "ComfyUI 插件性能优化专家"；visualize → "ComfyUI 插件功能分析专家" | 智能体 |

### 会话与文件

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ 会话多选管理 | CRUD 完整（创建/切换/删除/列表/改名） | 全链路 |
| ✅ 会话持久化 | JSON 文件存储，重启后历史消息不丢失 | 后端 |
| ✅ 会话标题编辑 API | `PUT /ai-coder/sessions/{id}/title`，支持 inline 改名持久化 | 后端 |
| ✅ 三界面独立会话管理 | 会话 JSON 新增 `type` 字段（develop/optimize/visualize），`GET /sessions?type=xxx` 按类型过滤；三个界面各有独立侧边栏会话列表，旧会话无 type 字段默认归入 develop | 全链路 |
| ✅ 会话删除双模式+二次确认 | 删除按钮触发确认对话框，提供「仅删除会话」与「删除会话和文件夹」两个选项，选项均需二次确认 | 全链路 |
| ✅ 消息编辑重生成 | 点击历史消息编辑按钮，修改内容后重新提交；后端接收 `truncate_at` 参数，删除该条以后的消息再生成新回复 | 全链路 |
| ✅ 设置面板 | 完整模型配置（来源、API地址、密钥、模型名、温度、max_tokens） | 全链路 |
| ✅ 设置面板 Tab 页签化 | 模型设置 / GitHub / 监控 / 云端同步 四个独立子页面（原"账号"Tab 已移至星星按钮独立登录面板） | 前端 |
| ✅ 文件附件上传 | 前端"+"按钮选文件 → base64 内联 → 后端解析存储 | 全链路 |
| ✅ 附件预览与管理 | 缩略图预览、文件名显示、删除附件 | 前端 |

### 踩坑记录与云端同步

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ 踩坑记录本地存储 | `数据/踩坑记录/` 目录下，文件名即 UUID，记录 `updated_at` ISO 8601 时间戳与 `author` 创建者账号（创建时从 JWT `sub` 获取，未登录为 `anonymous`；更新时保留原作者不变） | 后端 |
| ✅ 云端同步（上传 + 拉取） | 新增 `pull_from_cloud()` / `_pull_from_cloud_sync()` 方法，使用 HubApi 列出并下载 `踩坑记录/` JSON 文件，与本地增量合并 | 后端 |
| ✅ 冲突合并（updated_at 时序） | UUID 去重 + 比较 `updated_at` ISO 8601 字符串字典序（=时间序），保留较新版本 | 后端 |
| ✅ title 内容级去重 | 拉取前构建本地 title 索引，远端新条目若 title 与本地重复则跳过，返回值新增 `duplicates` 统计，防止不同 UUID 但同标题的重复条目 | 后端 |
| ✅ 删除墓碑机制 | 本地删除时创建 `{id}.deleted.json` 墓碑并标记同步；云端删除也采用墓碑预写入（先写入并同步 `{id}.deleted.json` 到 Dataset，再删除原始 .json），本地 pull 时能识别云端删除；墓碑 7 天过期自动清理；同步循环每轮先清理过期墓碑 → 上传脏文件 → 自动拉取云端 | 后端 |
| ✅ 手动云端拉取端点 | `POST /ai-coder/cloud/pull` 触发一次云端 → 本地拉取与合并 | 后端 |

### 插件开发工具

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ 插件脚手架创建 | 创建标准插件目录结构（含英文命名校验），`__init__.py` 模板内置 `WEB_DIRECTORY = "./界面与静态资源"` 并在 `__all__` 中导出 | 后端 |
| ✅ 模板市场 | 5 个内置模板、卡片式选择 UI、GET/POST /ai-coder/templates | 全链路 |
| ✅ 插件导出打包 | AST 节点提取、自动 README/pyproject.toml 生成、zip 打包 | 后端 |
| ✅ ComfyUI 路径自动获取 | 使用 folder_paths 获取 custom_nodes 和 models 路径 | 后端 |
| ✅ 功能可视化双模式 | 选择文件夹后从历史文件加载，操作栏切换「文件模式 / 功能模式」+「开始分析」按钮，左上角浮层切换显示已分析模式 | 全链路 |
| ✅ 功能模式 AI 语义分析 | 代码摘要 → prompt → 模型调用 → JSON 解析，按功能聚合节点 | 全链路 |
| ✅ 可视化结果持久化 | 插件目录 `可视化/` 文件夹存储 meta.json + 文件模式.json + 功能模式.json | 后端 |

### GitHub 集成

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ GitHub 同步上传 | 一键将项目代码同步到 GitHub（自动创建/更新仓库） | 全链路 |
| ✅ GitHub N+1 优化 | Git Trees API 批量查询 SHA、5 并发上传、断点续传、指数退避 | 后端 |
| ✅ Token 加密存储 | 使用 cryptography Fernet 对称加密敏感字段 | 后端 |
| ✅ GitHub 连接测试 | 设置面板中验证 Token 有效性 | 全链路 |

### 安全与认证

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ 后端认证体系清理 | 移除旧本地认证代码，认证功能完全由 RanKing 插件接管 | 后端 |
| ✅ 路径穿越防护 | resolve() + is_relative_to() 限制访问范围 | 后端 |
| ✅ 输入验证 | 名称长度限制（255字符）+ 保留字检测、会话标题 500 字符上限 | 后端 |
| ✅ 速率限制 | RateLimiter 60次/分钟/IP，超限返回 429 | 后端 |
| ✅ 错误响应标准化 | _error_response/_success_response 统一响应格式 | 后端 |
| ✅ JWT 认证 | 自实现 HMAC-SHA256 JWT、PBKDF2 密码哈希、双 Token（access 15min + refresh 7天） | 后端 |
| ✅ 用户目录隔离 | 不同用户数据存储隔离，向后兼容单用户 | 后端 |

### 监控与可观测

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ 审计日志 | JSONL 按天轮转、多条件查询、audit_middleware、30 天自动清理 | 后端 |
| ✅ 性能监控 | MetricsCollector（线程安全）、GET /ai-coder/metrics、5 秒刷新 | 全链路 |
| ✅ OpenAPI 文档 | 27 个端点完整 spec、Swagger UI（GET /ai-coder/docs） | 后端 |
| ✅ pytest 测试框架 | 73+ 单元测试，覆盖加密、路径安全、速率限制、输入验证 | 测试 |

### 扩展与部署

| 功能 | 说明 | 所在模块 |
|------|------|----------|
| ✅ 模型市场 | HuggingFace 搜索/详情/下载 API，纯 aiohttp 实现 | 后端 |
| ✅ Docker 容器化 | Dockerfile + docker-compose.yml + .dockerignore + 部署指南 | 部署 |
| ✅ 前端内存泄漏清理 | 事件清理注册表（registerCleanup/cleanupAllEvents）、定时器追踪、面板切换资源释放 | 前端 |
| ✅ 前端模块化重构 | 会话视图管理器拆分为 5 个模块，SSE 实时渲染 | 前端 |
| ✅ CSS 模块化拆分 | `样式表.css` 从 1900+ 行拆分为 14 个功能子模块（位于 `前端/样式/`），主文件仅含 @import 聚合入口 | 前端 |
| ✅ 知识库脚手架指南 | 新增 `知识库/基础节点开发/脚手架初始化与开发指南.md`（7 chunk），覆盖文件结构、`__init__.py` 详解、第一个节点流程、前端扩展、命名规范、requirements.txt | 知识库 |
| ✅ 知识库 Tab 目录隔离 | 知识库重组为 `develop/`（原全部插件开发文档）/ `optimize/`（占位）/ `visualize/`（占位）三个子目录，前端传 `activeTab` 字段，后端按 Tab 加载对应子目录 | 知识库 |
| ✅ 知识库云端→本地单向同步 | 以云端 Dataset 为权威源，只下载不上传：云端有本地无 → 下载；本地有云端无 → 保留本地不上传；哈希不同 → 始终取云端版本覆盖本地；`_上传本地文件()` 函数保留但不再调用 | 后端 |

---

## 部分实现 / 框架已有 ⚠️（2 项）

| 功能 | 当前状态 | 缺失部分 |
|------|----------|----------|
| ⚠️ 本地模型 GGUF 支持 | 已有 transformers 推理 | 缺 GGUF 格式支持（llama-cpp-python） |
| ⚠️ 代码高亮 | Markdown 代码块已有语言标签 | 缺完整语法高亮（仅基础样式） |

---

## 未实现功能 ❌（1 项）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| ❌ LLM 摘要压缩 | 用 LLM 总结早期对话保留关键信息 | 中 |

---

## 功能完成度统计

```
已实现： 78 项  ████████████████  97%
部分实现：2 项   ░░░░░░░░░░░░░░░░   2%
未实现：  1 项   ░░░░░░░░░░░░░░░░   1%
```

---

最后更新：2026-06-22（本次更新：本地模型工具调用适配层实现、工具调用循环上限提升至15轮、会话列表面板公共组件、插件文件夹与会话互斥选择、释放显存按钮三面板统一、会话标题栏取消按钮、优化/可视化面板搜索框移除、后端认证体系清理）
