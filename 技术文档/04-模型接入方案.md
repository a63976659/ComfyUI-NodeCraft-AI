# 模型接入方案

> 本文档描述双通道模型接入架构、设置界面设计和实现规划。  
> 最后更新时间：2026-06-12

---

## 当前实现状态

| 维度 | 状态 | 说明 |
|------|------|------|
| API 调用 | ✅ 完全实现 | 支持 OpenAI 兼容 API，可配置 base_url/model/key |
| 本地模型 | ✅ 基础实现 | transformers 推理，待支持 GGUF |
| 设置 UI | ✅ 完全实现 | 完整设置面板，模型来源切换 |
| 配置存储 | ✅ JSON 持久化 | 数据/settings.json |
| 模型切换 | ✅ 已实现 | 本地/API 切换按钮 + 本地模型下拉选择 |
| API Key 加密 | ❌ 未实现 | 当前明文存储，待加密 |
| GGUF 支持 | ❌ 未实现 | 待集成 llama-cpp-python |

---

## 本地模型推理实现细节（LocalModelClient）

### 核心实现

```python
# 模型客户端.py 中的 LocalModelClient
from transformers import AutoModelForCausalLM, AutoTokenizer
import asyncio

class LocalModelClient:
    def __init__(self):
        self.model = None
        self.tokenizer = None
        self.current_model_path = None

    def load_model(self, model_path):
        """加载本地模型，自动 GPU 分配"""
        # 卸载旧模型释放显存
        if self.model is not None:
            del self.model
            del self.tokenizer
            torch.cuda.empty_cache()
        
        self.tokenizer = AutoTokenizer.from_pretrained(model_path)
        self.model = AutoModelForCausalLM.from_pretrained(
            model_path,
            device_map="auto",      # 自动 GPU 分配
            torch_dtype="auto"       # 自动精度选择
        )
        self.current_model_path = model_path

    async def generate(self, messages, **kwargs):
        """异步推理，避免阻塞事件循环"""
        loop = asyncio.get_event_loop()
        result = await loop.run_in_executor(
            None,
            self._sync_generate,
            messages,
            kwargs
        )
        return result

    def _sync_generate(self, messages, kwargs):
        """同步推理实现"""
        inputs = self.tokenizer.apply_chat_template(
            messages, return_tensors="pt"
        ).to(self.model.device)
        outputs = self.model.generate(inputs, **kwargs)
        return self.tokenizer.decode(outputs[0], skip_special_tokens=True)
```

### 关键技术点

| 技术点 | 实现方式 | 说明 |
|--------|----------|------|
| 模型加载 | `AutoModelForCausalLM.from_pretrained(path, device_map="auto")` | 自动分配 GPU/CPU |
| 异步推理 | `asyncio.run_in_executor` | 避免阻塞 aiohttp 事件循环 |
| 模型切换 | 卸载旧模型 + `torch.cuda.empty_cache()` | 释放显存后加载新模型 |
| 精度管理 | `torch_dtype="auto"` | 根据模型配置自动选择 |

---

## 双通道模型架构

```
┌─────────────────────────────────────────┐
│              模型管理器                    │
├──────────────────┬──────────────────────┤
│   通道A: 本地模型  │   通道B: API 模型     │
├──────────────────┼──────────────────────┤
│ • GGUF 格式       │ • OpenAI 兼容 API    │
│ • 本地推理引擎     │ • 自定义 base_url    │
│ • 零网络依赖      │ • 多提供商支持        │
└──────────────────┴──────────────────────┘
         ↕ 用户在设置面板选择切换 ↕
```

---

## 通道A - 本地模型

### 模型存储路径

| 类型 | 路径 | 说明 |
|------|------|------|
| 默认路径 | `ComfyUI\models\LLM` | 根据 ComfyUI 安装位置自动定位 |
| 自定义路径 | 用户设置 | 用户可在设置面板中指定任意路径 |
| 恢复默认 | 一键操作 | 清除自定义配置，回到默认路径 |

### 路径获取逻辑

```python
# 系统环境映射.py 中已实现基础版
def get_default_llm_path():
    """获取默认 LLM 模型路径"""
    models_dir = folder_paths.models_dir  # ComfyUI models 目录
    return os.path.join(models_dir, "LLM")
```

### 支持的模型格式

- **GGUF**：主要支持格式，兼容 llama.cpp 生态
- 量化级别：Q4_K_M、Q5_K_M、Q8_0 等

### 本地推理引擎选型

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| llama-cpp-python | 纯 Python 绑定，易集成 | 需编译，依赖 C++ 环境 | 深度集成 |
| Ollama 本地 API | 独立服务，API 兼容 | 需额外安装 Ollama | 用户已有 Ollama |
| llama.cpp server | 轻量级 HTTP 服务 | 需手动启动 | 灵活部署 |

**推荐方案**：优先支持 llama-cpp-python（深度集成），同时兼容 Ollama API（作为本地 API 模式）。

---

## 通道B - API 模型

### 当前实现

```python
# 模型客户端.py 中已实现
class AICoderClient:
    def __init__(self):
        self.base_url = "http://localhost:11434/v1"  # 默认 Ollama
        self.model = "qwen2.5-coder-32b-instruct"
        self.temperature = 0.2
```

### 目标实现

支持配置项：
- `base_url`：API 端点地址
- `model_name`：模型名称
- `api_key`：API 密钥（加密存储）
- `temperature`：生成温度
- `max_tokens`：最大输出 token 数

### 支持的 API 提供商

| 提供商 | base_url 示例 | 典型模型 |
|--------|---------------|----------|
| OpenAI | `https://api.openai.com/v1` | gpt-4o, gpt-4o-mini |
| DeepSeek | `https://api.deepseek.com/v1` | deepseek-coder, deepseek-chat |
| 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | qwen-coder-plus |
| Ollama (本地) | `http://localhost:11434/v1` | 任意本地模型 |
| 自定义 | 用户填写 | 任意 OpenAI 兼容模型 |

### API Key 安全存储

```
存储位置: ComfyUI-NodeCraft-AI/数据/config.json（加密）
加密方式: 本地密钥 + AES 对称加密（或使用 keyring 库）
读取流程: 启动时解密加载到内存，不在日志中输出
```

---

## 模型切换逻辑

```
用户打开设置面板
    │
    ├─ 选择 "本地模型"
    │     │
    │     ├─ 读取模型路径下的 .gguf 文件列表
    │     ├─ 用户选择模型文件
    │     └─ 启动本地推理引擎
    │
    └─ 选择 "API 模型"
          │
          ├─ 填写 base_url
          ├─ 填写 model_name
          ├─ 填写 API Key
          └─ 测试连接
```

---

## 设置界面设计

### 布局结构

```
┌─────────────── 设置面板 ───────────────┐
│                                         │
│  ┌─ 模型来源 ─────────────────────────┐ │
│  │  ○ 本地模型    ● API 模型          │ │
│  └─────────────────────────────────────┘ │
│                                         │
│  ┌─ API 模型配置 ─────────────────────┐ │
│  │  API 地址:  [____________________] │ │
│  │  模型名称:  [____________________] │ │
│  │  API Key:   [____________________] │ │
│  └─────────────────────────────────────┘ │
│                                         │
│  ┌─ 本地模型配置 ─────────────────────┐ │
│  │  模型路径:  [________] [浏览] [默认]│ │
│  │  模型文件:  [▼ 下拉选择 _________ ]│ │
│  └─────────────────────────────────────┘ │
│                                         │
│  ┌─ 通用参数 ─────────────────────────┐ │
│  │  Temperature: [0.2____]            │ │
│  │  Max Tokens:  [4096___]            │ │
│  └─────────────────────────────────────┘ │
│                                         │
│        [测试连接]    [保存设置]          │
└─────────────────────────────────────────┘
```

### 设置项说明

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 模型来源 | radio | API | 本地模型 / API 模型 |
| API 地址 | text | `http://localhost:11434/v1` | OpenAI 兼容 API endpoint |
| 模型名称 | text | `qwen2.5-coder-32b-instruct` | 模型标识符 |
| API Key | password | 空 | 加密存储 |
| 本地模型路径 | path | `ComfyUI\models\LLM` | 模型文件所在目录 |
| 本地模型文件 | select | 空 | 从路径中扫描的 .gguf 文件 |
| Temperature | number | 0.2 | 0.0 ~ 2.0 |
| Max Tokens | number | 4096 | 最大输出长度 |

---

## 当前实现与目标差距分析

| 维度 | 当前状态 | 目标状态 | 差距 |
|------|----------|----------|------|
| API 调用 | ✅ 完全实现（可配置） | 可配置 base_url/model/key | 已完成 |
| 本地模型 | ✅ 基础实现（transformers） | GGUF 本地推理 | 需集成 llama-cpp-python |
| 设置 UI | ✅ 完整设置面板 | 完整设置面板 | 已完成 |
| 配置存储 | ✅ JSON 持久化 | JSON 加密存储 | 需添加加密 |
| 模型切换 | ✅ 本地/API 切换 | 本地/API 切换 | 已完成 |
| 路径管理 | ✅ 默认路径 + 扫描 | 默认+自定义+恢复 | 基本完成 |

---

最后更新：2026-06-12
